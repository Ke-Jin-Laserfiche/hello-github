; begin auth token
//registry.npmjs.org/:username=${{ secrets.NPM_TOKEN }}
//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}
//registry.npmjs.org/:email=npm requires email to be set but doesn't use the value
; end auth token 