; begin auth token
@lf-ke-jin://registry.npmjs.org/:username=${{ secrets.NPM_USERNAME }}
@lf-ke-jin://registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}
@lf-ke-jin://registry.npmjs.org/:email=npm requires email to be set but doesn't use the value
; end auth token 